#!/usr/bin/python3"
"""Default configuration values for synthimage package."""

DEFAULT_IMAGE_SIZE = (224, 224)  # default size for ResNet (can be changed)
DEFAULT_OUTPUT_FORMAT = "PNG"
DEFAULT_LABELS_DIR = "generated_images"
